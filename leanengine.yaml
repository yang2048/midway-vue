systemDependencies:
  - midway-bin
installDevDependencies: true
install: npm install && npm install @midwayjs/cli -g
build: npm run build
prune: npm prune --production  
#run: $(npm bin)/serve -c static.json -l ${LEANCLOUD_APP_PORT}
run: npm run start