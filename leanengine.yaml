# installDevDependencies: true
# install:
#   - echo 'building'
#   - yarn install
build: 
  - require:
      - build/
      - dist/
  - { use: "default" }
  - { use: "midway-bin" }
  - yarn run build
run: yarn run start